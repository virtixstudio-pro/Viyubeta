<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#0A0A0A"
    android:gravity="center"
    android:padding="24dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Choisis ton pseudo unique"
        android:textColor="#FFFFFF"
        android:textSize="20sp"
        android:textStyle="bold"
        android:layout_marginBottom="8dp"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Il doit obligatoirement commencer par @"
        android:textColor="#888888"
        android:textSize="14sp"
        android:layout_marginBottom="24dp"
        android:gravity="center"/>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <EditText
            android:id="@+id/et_pseudo"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:background="@drawable/bg_input_rounded"
            android:hint="@pseudo"
            android:textColorHint="#555555"
            android:textColor="#FFFFFF"
            android:paddingStart="16dp"
            android:paddingEnd="50dp"
            android:inputType="text"
            android:text="@"/>

        <TextView
            android:id="@+id/tv_pseudo_status"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:layout_marginEnd="16dp"
            android:text="❌"
            android:textSize="18sp"/>
    </RelativeLayout>

    <TextView
        android:id="@+id/tv_pseudo_suggestion"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="24dp"
        android:layout_gravity="start"
        android:text="Suggestion : @chargement..."
        android:textColor="#00FFCC"
        android:textSize="13sp"
        android:textStyle="italic"
        android:visibility="gone"/>

    <Button
        android:id="@+id/btn_valider_pseudo"
        android:layout_width="match_parent"
        android:layout_height="45dp"
        android:background="@drawable/bg_button_rounded"
        android:text="Valider"
        android:textColor="#0A0A0A"
        android:textStyle="bold"
        android:textAllCaps="false"
        android:layout_marginTop="16dp"/>

    <ProgressBar
        android:id="@+id/pb_pseudo_loading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:visibility="gone"/>

</LinearLayout>
