<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#0A0A0A"
    android:padding="16dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Paramètres de Viyu"
        android:textColor="#FFFFFF"
        android:textSize="22sp"
        android:textStyle="bold"
        android:layout_marginBottom="24dp"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Personnalisation de l'interface"
        android:textColor="#888888"
        android:textSize="14sp"
        android:layout_marginBottom="16dp"/>

    <Button
        android:id="@+id/btn_theme_toggle"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:background="@drawable/bg_input_rounded"
        android:text="Changer le Thème (Sombre / Clair)"
        android:textColor="#FFFFFF"
        android:layout_marginBottom="12dp"
        android:textAllCaps="false"/>

    <Button
        android:id="@+id/btn_font_size"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:background="@drawable/bg_input_rounded"
        android:text="Taille de la police du chat"
        android:textColor="#FFFFFF"
        android:layout_marginBottom="12dp"
        android:textAllCaps="false"/>

    <Button
        android:id="@+id/btn_bubble_color"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:background="@drawable/bg_input_rounded"
        android:text="Couleur des bulles de message"
        android:textColor="#FFFFFF"
        android:textAllCaps="false"/>

</LinearLayout>
